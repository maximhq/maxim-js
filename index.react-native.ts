// React Native entry point 
// Set environment variable to force React Native platform BEFORE any imports
process.env['MAXIM_PLATFORM'] = 'react-native';

// Re-export everything from main index
export * from './index';